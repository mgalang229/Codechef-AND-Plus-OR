#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	int tt;
	cin >> tt;
	while (tt--) {
		long long x;
		cin >> x;
		// view the image in this repository for better understanding
		// output 0 and 'x' itself (always valid)
		cout << 0 << " " << x << '\n';
	}
	return 0;
}
